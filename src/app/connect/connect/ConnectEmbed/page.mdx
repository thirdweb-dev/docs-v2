import {
	SDKCard,
	Grid,
	DocImage,
	GithubTemplateCard,
	Stack,
	createMetadata,
} from "@doc";
import { ReactIcon } from "@/icons";
import connectHeroImage from "../assets/ConnectEmbed-embedded.png";

export const metadata = createMetadata({
	image: {
		title: "ConnectEmbed",
		icon: "react",
	},
	title: "thirdweb ConnectEmbed",
	description:
		"ConnectEmbed component renders a UI to connect to various wallets",
});

# ConnectEmbed

ConnectEmbed component renders a UI to connect to various wallets.

It renders the same UI as the [ConnectButton](/connect/connect/ConnectButton) component's Modal UI. This is useful for Implementing a "Sign in" page.

<Grid>
	<SDKCard
		title="React"
		href="/typescript/v5/react/components/ConnectEmbed"
		icon={ReactIcon}
	/>
</Grid>

<br />

<DocImage src={connectHeroImage} />

## Templates

<Stack>

<GithubTemplateCard
	title="Using ConnectEmbed to create a sign-in page in Next.js"
	href="https://github.com/thirdweb-example/connect-embed-next"
	tag="v4 SDK"
/>

<GithubTemplateCard
	title="ConnectEmbed usage in React Native"
	href="https://github.com/thirdweb-example/react-native-connect-embed"
	tag="v4 SDK"
/>

</Stack>
