import { Tabs, TabsList, TabsTrigger, TabsContent } from "@/components/ui/tabs";
import { createMetadata } from "@doc";

export const metadata = createMetadata({
	image: {
		title: "Interact with In-App Wallet",
		icon: "wallets",
	},
	title: "Interact with In-App Wallet",
	description:
		"Learn how to interact with the in-app wallet using the React hooks or the EmbeddedWallet instance.",
});

# Interact with wallets

Once connected, you can interact with the wallet using the `thirdweb/wallets/embedded` specific methods.

## Get the user email

```typescript
import { getUserEmail } from "thirdweb/wallets/embedded";

const email = await getUserEmail({ client });
console.log("user email", email);
```

## Get the user's account

<Tabs defaultValue="react">

<TabsList>
	<TabsTrigger value="react">React</TabsTrigger>
	<TabsTrigger value="typecsript">TypeScript</TabsTrigger>
</TabsList>

<TabsContent value="react">

```typescript
import { useActiveAccount } from "thirdweb/react";

const account = useActiveAccount();
```

</TabsContent>

<TabsContent value="typecsript">
```typescript
const account = await embeddedWallet.getAccount();
```

</TabsContent>

</Tabs>

## Get the user's wallet address

<Tabs defaultValue="react">

<TabsList>
	<TabsTrigger value="react">React</TabsTrigger>
	<TabsTrigger value="typecsript">TypeScript</TabsTrigger>
</TabsList>

<TabsContent value="react">

```typescript
import { useActiveAccount } from "thirdweb/react";

const account = useActiveAccount();
console.log("wallet address", account.address);
```

</TabsContent>

<TabsContent value="typecsript">

```typescript
const account = embeddedWallet.getAccount();
const address = account.address;
```

</TabsContent>

</Tabs>

## Get wallet balance

```typescript
await getWalletBalance({
	client,
	chain,
	address: account.address,
});
```

## Sign a message

```typescript
await embeddedWallet.sign("Hello World!");
```
