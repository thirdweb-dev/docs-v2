import { DocImage } from "@doc";
import OpenFaucetImage from "../images/details-open-faucet-dark.png";

Hide the "Request Testnet funds" link in ConnectWallet dropdown which is shown when user is connected to a testnet.

Default is `false`

```jsx
<ConnectWallet hideTestnetFaucet={false} />
```

<DocImage src={OpenFaucetImage} className="border rounded-lg" />
