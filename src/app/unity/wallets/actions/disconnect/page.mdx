import { createMetadata } from "@doc";

export const metadata = createMetadata({
	title: "wallet.Disconnect | thirdweb Unity SDK",
	description: "Disconnect a user's wallet from your game",
});

# Disconnect

Disconnect a user's wallet from your game.

## Usage

```csharp
await sdk.wallet.Disconnect();
```
